// document ready
$(function() {
    $("#accordion a.card-link").on("click", function(event){
        // // liste les cartes ouverte en dehors de celle selectionee
        // let cards = $("#accordion a.card-link");
        // cards.remove(this);
        // console.log(cards);
        // cards.toggleClass(".collapsed");
    });
});
